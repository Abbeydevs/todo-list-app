import React from 'react'

const Form = () => {

    //Things I need to do here
    //Get Input value
    // const getInputValue = (e) => {
    //     console.log(e.target.value
        
    // }

    //Submit the form
    const handleSubmit = (e) => {
        console.log(e.target.value)
    }


  return (
    <div>Form component is working</div>
  )
}

export default Form;
